# 🎯 Рекомендации по завершению миграции на DDD

## 📊 Текущая ситуация

### ✅ Что уже хорошо
- **70% кода мигрировано на DDD**
- Portfolio, Recommendation, Price History - полностью DDD
- Stock Analysis - частично (есть use case, но legacy код всё ещё используется)
- Есть DI контейнер и правильная структура

### ⚠️ Проблемы
- **Смешанная архитектура**: часть кода DDD, часть legacy
- **Дублирование**: `ReportGenerator` и `GenerateReportUseCase` делают одно и то же
- **Сложность поддержки**: непонятно, какой код использовать

---

## 🎯 Рекомендация: **Продолжить миграцию**

### Почему стоит продолжить:

1. **Уже проделана большая работа (70%)**
   - Осталось только завершить Stock Analysis и мигрировать Event Prediction
   - Инфраструктура готова (DI, репозитории, use cases)

2. **Преимущества уже видны**
   - Чёткое разделение слоёв
   - Легко тестировать (можно мокать репозитории)
   - Легко расширять (добавлен Price History Context без проблем)

3. **Смешанная архитектура создаёт проблемы**
   - Дублирование логики
   - Сложнее понять, какой код использовать
   - Риск багов при изменении

---

## 📋 План действий (приоритизированный)

### 🔴 Критично (сделать сейчас)

#### 1. Завершить миграцию Stock Analysis Context

**Задача:** Полностью перейти на `GenerateReportUseCase` вместо `ReportGenerator`

**Шаги:**
1. Изменить `DailyJobScheduler` - использовать DDD по умолчанию
2. Проверить, что `GenerateReportUseCase` покрывает все сценарии
3. Обновить все места использования
4. Удалить или пометить `ReportGenerator` как deprecated

**Оценка:** 2-3 дня

**Файлы для изменения:**
- `app/scheduler/daily_job.py` - изменить `use_ddd=False` → `use_ddd=True` по умолчанию
- `app/api/server.py` - проверить, что использует use case
- `app/process/report.py` - пометить как deprecated или удалить

#### 2. Унифицировать MetricsCalculator

**Задача:** Использовать только DDD версию

**Шаги:**
1. Убедиться, что `app/domain/stock_analysis/services/metrics_calculator.py` содержит всю логику
2. Удалить `app/process/metrics.py` или сделать его алиасом
3. Обновить импорты

**Оценка:** 1 день

---

### 🟡 Важно (следующие 2-4 недели)

#### 3. Мигрировать Event Prediction Context

**Задача:** Создать DDD структуру для предсказания событий

**Оценка:** 2-3 недели

**Структура:**
```
app/domain/event_prediction/
├── entities/
│   └── event_prediction.py
├── value_objects/
│   ├── event_type.py
│   └── prediction_confidence.py
├── services/
│   └── event_predictor.py
└── repositories/
    └── event_prediction_repository.py

app/application/event_prediction/
├── predict_events_use_case.py
└── analyze_news_use_case.py
```

#### 4. Рефакторинг Infrastructure

**Задача:** Изолировать внешние зависимости

**Шаги:**
1. Переместить `app/ingest/moex_client.py` → `app/infrastructure/external/moex/`
2. Убедиться, что `MOEXClient` используется только через репозитории
3. Унифицировать хранилище (все операции через репозитории)

**Оценка:** 1 неделя

---

### 🟢 Желательно (когда будет время)

#### 5. Очистка legacy кода

**Задача:** Удалить неиспользуемые файлы

**Оценка:** 1-2 дня

#### 6. Улучшение документации

**Задача:** Обновить документацию по новой архитектуре

**Оценка:** 1-2 дня

---

## 🚀 Быстрый старт (что сделать прямо сейчас)

### Шаг 1: Переключить планировщик на DDD

```python
# app/scheduler/daily_job.py
def __init__(self, use_ddd: bool = True):  # Изменить False → True
```

### Шаг 2: Проверить, что всё работает

```bash
# Запустить тесты
pytest tests/

# Запустить планировщик вручную
python run_job_once.py
```

### Шаг 3: Если всё работает - удалить legacy код

```python
# app/process/report.py - пометить как deprecated
# или удалить, если не используется
```

---

## ⚠️ Риски и как их минимизировать

### Риск 1: Баги при миграции
**Минимизация:**
- Тестировать каждый шаг
- Сохранить legacy код на время переходного периода
- Использовать feature flags

### Риск 2: Временные затраты
**Минимизация:**
- Делать постепенно (по одному контексту)
- Не переписывать всё сразу
- Использовать адаптеры для обратной совместимости

### Риск 3: Обучение команды
**Минимизация:**
- Документировать изменения
- Проводить code review
- Использовать примеры

---

## 📊 Метрики успеха

### После завершения миграции:

- ✅ **100% кода в DDD структуре**
- ✅ **Нет дублирования логики**
- ✅ **Все зависимости через DI**
- ✅ **Легко тестировать** (можно мокать репозитории)
- ✅ **Легко расширять** (новые контексты добавляются просто)

---

## 💡 Итоговая рекомендация

**Продолжить миграцию**, потому что:

1. ✅ Уже 70% сделано
2. ✅ Преимущества видны
3. ✅ Осталось немного работы
4. ✅ Смешанная архитектура создаёт проблемы

**Но делать постепенно:**
- Сначала завершить Stock Analysis (2-3 дня)
- Потом Event Prediction (2-3 недели)
- Затем рефакторинг Infrastructure (1 неделя)

**Не переписывать всё сразу** - это рискованно.

---

## 🎯 Следующий шаг

**Рекомендую начать с:**
1. Изменить `DailyJobScheduler(use_ddd=True)` по умолчанию
2. Протестировать
3. Если всё работает - удалить legacy `ReportGenerator`

Это займёт 1-2 часа и сразу улучшит архитектуру.

---

*Документ создан: 2026-01-27*
